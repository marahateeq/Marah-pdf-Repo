name: PR Title Validation
on:
  pull_request:
    types: [opened, edited, reopened, synchronize, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  validate:
    uses: marahateeq/.github/workflows/pr-title-validation.yml@main
    with:
      ticket-prefix: "KAN" # Optional, defaults to KAN
